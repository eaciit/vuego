# VueGo

A Vue + Golang Project

## Build Setup

``` bash
# install dependencies
npm install

# build for production with minification
npm run build

# build for watch
npm run watch

# run app
cd demoapp
go run *.go --host=:9123 
```